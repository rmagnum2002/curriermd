<h5>Articles connexes</h5>
<% for art in @related_articles %>
  <div class="span">
    <%= link_to image_tag(art.avatar.url(:thumb), size: "100x100"), art %>
    <h6 style="width:100px;"><%= link_to (truncate(art.title, omision: "...", length: 50)), art %></h6>
  </div>
<% end %>
